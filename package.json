{
  "name": "tokenpool_sepolia",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ davidazovsky/tokenpool_sepolia",
    "create-local": "graph create --node http://localhost:8020/ davidazovsky/tokenpool_sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ davidazovsky/tokenpool_sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 davidazovsky/tokenpool_sepolia",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "graph-cli": "^0.0.1",
    "matchstick-as": "0.5.0"
  }
}
